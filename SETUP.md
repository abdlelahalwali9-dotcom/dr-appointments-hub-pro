# دليل الإعداد الكامل 🛠️

## المرحلة 1: الإعداد المحلي

### المتطلبات
- Node.js 18+ ([تحميل](https://nodejs.org))
- pnpm 10+ ([تحميل](https://pnpm.io))
- Git ([تحميل](https://git-scm.com))
- MySQL 8+ أو قاعدة بيانات سحابية

### التثبيت

```bash
# 1. استنساخ المستودع
git clone https://github.com/abdlelahalwali9-dotcom/dr-appointments-hub-pro.git
cd dr-appointments-hub-pro

# 2. تثبيت الاعتماديات
pnpm install

# 3. إعداد قاعدة البيانات
# أنشئ ملف .env بالمتغيرات المطلوبة
cp .env.example .env

# 4. إنشاء جداول قاعدة البيانات
pnpm db:push

# 5. زرع البيانات التجريبية
pnpm seed

# 6. تشغيل خادم التطوير
pnpm dev
```

### الوصول إلى التطبيق
- الواجهة الأمامية: http://localhost:3000
- API: http://localhost:3000/api/trpc

---

## المرحلة 2: إعداد قاعدة البيانات السحابية

### خيار 1: Supabase (الموصى به)

**المميزات:**
- ✅ قاعدة بيانات PostgreSQL مجانية
- ✅ مصادقة مدمجة
- ✅ تخزين ملفات
- ✅ دعم عربي جيد

**الخطوات:**

```bash
# 1. اذهب إلى https://supabase.com
# 2. انقر "Start Your Project"
# 3. اختر "Create a new project"
# 4. أدخل التفاصيل:
#    - Project Name: dr-appointments-hub
#    - Database Password: (اختر كلمة مرور قوية)
#    - Region: (اختر الأقرب إليك)

# 5. انسخ DATABASE_URL من الإعدادات
# 6. أضفها إلى ملف .env:
DATABASE_URL="postgresql://user:password@host:5432/postgres"

# 7. قم بتشغيل الترحيل
pnpm db:push

# 8. زرع البيانات
pnpm seed
```

### خيار 2: MySQL Cloud

**المميزات:**
- ✅ قاعدة بيانات MySQL مجانية
- ✅ سهل الاستخدام
- ✅ دعم جيد

**الخطوات:**

```bash
# 1. اذهب إلى https://www.freemysqlhosting.net أو
#    https://www.db4free.net

# 2. أنشئ حساب جديد

# 3. انسخ بيانات الاتصال

# 4. أضفها إلى ملف .env:
DATABASE_URL="mysql://user:password@host:3306/database"

# 5. قم بتشغيل الترحيل
pnpm db:push

# 6. زرع البيانات
pnpm seed
```

### خيار 3: PlanetScale

**المميزات:**
- ✅ MySQL متوافق
- ✅ مجاني
- ✅ سريع جداً

**الخطوات:**

```bash
# 1. اذهب إلى https://planetscale.com
# 2. أنشئ حساب جديد
# 3. أنشئ قاعدة بيانات جديدة
# 4. انسخ DATABASE_URL
# 5. أضفها إلى ملف .env
# 6. قم بتشغيل الترحيل والزرع
```

---

## المرحلة 3: النشر على Vercel

### الخطوات

```bash
# 1. اذهب إلى https://vercel.com
# 2. انقر "Sign Up" واختر GitHub
# 3. اختر المستودع: dr-appointments-hub-pro
# 4. أضف متغيرات البيئة (انظر أدناه)
# 5. انقر "Deploy"
```

### متغيرات البيئة المطلوبة

```
DATABASE_URL=your-database-url
JWT_SECRET=your-secret-key
VITE_APP_ID=your-app-id
OAUTH_SERVER_URL=https://api.manus.im
VITE_OAUTH_PORTAL_URL=https://auth.manus.im
OWNER_OPEN_ID=your-owner-id
OWNER_NAME=Your Name
BUILT_IN_FORGE_API_URL=https://api.manus.im
BUILT_IN_FORGE_API_KEY=your-api-key
VITE_FRONTEND_FORGE_API_KEY=your-frontend-key
VITE_FRONTEND_FORGE_API_URL=https://api.manus.im
```

---

## المرحلة 4: الاختبار

### اختبار المحلي

```bash
# 1. تشغيل خادم التطوير
pnpm dev

# 2. فتح المتصفح على http://localhost:3000

# 3. اختبار الميزات:
#    - تسجيل الدخول
#    - إضافة مريض
#    - إضافة موعد
#    - عرض التقارير
#    - إلخ
```

### اختبار الإنتاج

```bash
# 1. بناء المشروع
pnpm build

# 2. تشغيل الإنتاج محلياً
pnpm start

# 3. فتح المتصفح على http://localhost:3000

# 4. اختبار جميع الميزات
```

---

## المرحلة 5: الأمان

### خطوات الأمان

1. **تغيير كلمات المرور**
   - غير كلمة مرور قاعدة البيانات
   - غير JWT_SECRET
   - غير جميع مفاتيح API

2. **تفعيل HTTPS**
   - Vercel يفعله تلقائياً
   - استخدم HTTPS فقط في الإنتاج

3. **المصادقة الثنائية**
   - فعّل على GitHub
   - فعّل على Vercel
   - فعّل على قاعدة البيانات

4. **النسخ الاحتياطية**
   - نسخ احتياطية يومية لقاعدة البيانات
   - احفظ متغيرات البيئة بأمان

---

## استكشاف الأخطاء

### خطأ: "Cannot find module"
```bash
# الحل: إعادة تثبيت الاعتماديات
pnpm install
```

### خطأ: "Database connection failed"
```bash
# الحل: تحقق من DATABASE_URL
# تأكد من أن الخادم متاح
# تحقق من الصلاحيات
```

### خطأ: "Port 3000 is already in use"
```bash
# الحل: استخدم منفذ مختلف
PORT=3001 pnpm dev
```

### خطأ: "Build failed on Vercel"
```bash
# الحل:
# 1. تحقق من السجلات
# 2. تأكد من متغيرات البيئة
# 3. أعد محاولة النشر
```

---

## الأوامر المهمة

```bash
# التطوير
pnpm dev              # تشغيل خادم التطوير
pnpm build            # بناء المشروع
pnpm start            # تشغيل الإنتاج

# قاعدة البيانات
pnpm db:push          # ترحيل قاعدة البيانات
pnpm seed             # زرع البيانات التجريبية

# الاختبار
pnpm test             # تشغيل الاختبارات
pnpm check            # التحقق من TypeScript

# التنسيق
pnpm format           # تنسيق الكود
```

---

## الموارد المفيدة

- [React Documentation](https://react.dev)
- [TypeScript Documentation](https://www.typescriptlang.org)
- [TailwindCSS Documentation](https://tailwindcss.com)
- [tRPC Documentation](https://trpc.io)
- [Drizzle ORM Documentation](https://orm.drizzle.team)
- [Vercel Documentation](https://vercel.com/docs)

---

## الدعم

للمساعدة والاستفسارات:
- 📧 البريد: info@clinic.com
- 📞 الهاتف: +966 50 000 0000
- 🐙 GitHub Issues: [اضغط هنا](https://github.com/abdlelahalwali9-dotcom/dr-appointments-hub-pro/issues)

---

تم تطويره بـ ❤️ لتحسين الخدمات الطبية
